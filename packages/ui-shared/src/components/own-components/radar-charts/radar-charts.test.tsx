import { describe, it, expect } from 'vitest';

describe('ChartRadarDots', () => {

    it('first test', () =>{
        expect(true).toBe(true)
    })

    // it('renders the chart title', () => {
    //     render(<ChartRadarDots />)
    //     expect(screen.getByText('Radar Chart - Dots'))
    // })

    // it('renders the chart description', () => {
    //     render(<ChartRadarDots />)
    //     expect(screen.getByText('Showing total visitors for the last 6 months')).toBeInTheDocument()
    // })

    // it('renders the trending information', () => {
    //     render(<ChartRadarDots />)
    //     expect(screen.getByText(/Trending up by 5.2% this month/)).toBeInTheDocument()
    // })

    // it('renders the date range', () => {
    //     render(<ChartRadarDots />)
    //     expect(screen.getByText('January - June 2024')).toBeInTheDocument()
    // })

    // it('renders the card component', () => {
    //     const { container } = render(<ChartRadarDots />)
    //     expect(container.querySelector('[data-testid], .card, [class*="card"]')).toBeInTheDocument()
    // })

    // it('renders the chart container', () => {
    //     const { container } = render(<ChartRadarDots />)
    //     expect(container.querySelector('svg')).toBeInTheDocument()
    // })

    // it('has the correct aspect ratio class', () => {
    //     const { container } = render(<ChartRadarDots />)
    //     expect(container.querySelector('.aspect-square')).toBeInTheDocument()
    // })

    // it('has the correct max height class', () => {
    //     const { container } = render(<ChartRadarDots />)
    //     expect(container.querySelector('.max-h-\\[250px\\]')).toBeInTheDocument()
    // })
})